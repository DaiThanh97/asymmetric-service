export const { ASYMMETRIC_DB_NAME = 'asymmetric' } = process.env;
export const { ASYMMETRIC_LOG_DB_NAME = 'log' } = process.env;

export const { MONGO_USERNAME = 'log' } = process.env;
export const { MONGO_PASSWORD = 'log' } = process.env;
export const { MONGO_HOST = 'log' } = process.env;
export const { MONGO_PORT = 'log' } = process.env;